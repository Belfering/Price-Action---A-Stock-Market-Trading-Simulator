POSTGRES_DB=price_action
POSTGRES_USER=price_action
POSTGRES_PASSWORD=replace_with_a_long_random_password

DEFAULT_USERNAME=1
DEFAULT_PASSWORD=1

PUBLIC_DOMAIN=tradingsimulator.io

# Keep false for IP-only SSH-tunnel staging. Set true only behind HTTPS.
COOKIE_SECURE=false
SESSION_DAYS=30
